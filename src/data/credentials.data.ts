import { TCredentials } from '../types/TCredentials';

export const credentials = {
  standard_user: { username: 'standart_user', password: 'secret_sauce' } as TCredentials,
  visual_user: { username: 'visual_user', password: 'secret_sauce' } as TCredentials
};
